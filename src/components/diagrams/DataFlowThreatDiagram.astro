<figure class="figma-diagram" aria-labelledby="dfd-threat-caption">
  <img
    class="figma-diagram-image"
    src="/diagrams/beaconguard-dfd-trust-boundaries.svg"
    alt="BeaconGuard data-flow diagram separating context, AI proposal, exact-action authorization, execution, governing policy authority, authorization evidence, and independent verification into distinct trust boundaries."
    loading="lazy"
    decoding="async"
  />
  <figcaption id="dfd-threat-caption">
    Each boundary establishes a different kind of authority. Identity, delegation, and invocation access do not silently become permission for the final consequential side effect.
  </figcaption>
</figure>
<style>
  .figma-diagram { margin:1.5rem 0; }
  .figma-diagram-image { display:block; width:100%; height:auto; border-radius:1rem; }
  figcaption { margin-top:.7rem; color:var(--text-muted); font-size:.9rem; }
</style>
